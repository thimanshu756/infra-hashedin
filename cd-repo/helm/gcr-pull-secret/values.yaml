namespaces:
  - frontend
  - backend-users
  - backend-products
  - backend-orders

# Base64-encoded Docker config JSON for GCR authentication
# This value is set by scripts/create-gcr-secret.sh or via SealedSecret in Phase 4
# NEVER commit the actual value to Git
dockerconfigjson: ""
